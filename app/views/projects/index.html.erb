<table>
  <tbody>
    <tr>
      <th>Name</th>
      <th>Open Tickets</th>
      <th>Total Tickets</th>
    </tr>
    <% @projects.each do |project| %>
    <tr>
      <td>
        <dl>
          <dt><%= link_to project.name, project_path(project) %></dt>
          <dd><%= project.description %></dd>
        </dl>
      </td>
      <td>0</td>
      <td>0</td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New Project', new_project_path %>